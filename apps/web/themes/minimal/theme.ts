import type { Skin } from "../types";

export const minimalSkin: Skin = {
  id: "minimal",
  name: "Minimal",
  description:
    "Square corners, a narrower shell and a quieter palette. Exists to prove the seams are real — if a value can't be changed here, it isn't a token yet.",
};
